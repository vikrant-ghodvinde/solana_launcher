import type { NextPage } from "next";
import { LaunchedTokensView } from "views/launchedTokens";

const LaunchedTokens: NextPage = (props) => {
  return (
    <div>
      <LaunchedTokensView />
    </div>
  );
};
export default LaunchedTokens;