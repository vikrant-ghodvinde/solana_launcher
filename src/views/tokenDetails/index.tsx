import { FC } from "react";
import { Container } from "components/Container/Container";
import { TokenDetails } from "components/TokenDetails";

export const TokenDetailsView: FC = ({}) => {
  return (
    <Container>
      <TokenDetails />
    </Container>
  );
};
